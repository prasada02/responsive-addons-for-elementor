const cards=gsap.utils.toArray(".rael-stacking-card");const lastIndex=cards.length-1;cards.forEach((card,index)=>{if(index===lastIndex)return;const baseX=parseFloat(card.dataset.translateX)||0;const baseY=parseFloat(card.dataset.translateY)||0;const baseRotate=parseFloat(card.dataset.rotate)||0;const baseScale=parseFloat(card.dataset.scale)||1;const baseBlur=parseFloat(card.dataset.blur)||0;const baseGrayscale=parseFloat(card.dataset.grayscale)||0.2;gsap.to(card,{scrollTrigger:{trigger:card,start:"top+=1 top",end:"+=100%",scrub:!0,},x:baseX+index*5,y:baseY+index*10,rotate:baseRotate+index*2,scale:baseScale-index*0.02,filter:`blur(${baseBlur + index * 0.5}px) grayscale(${baseGrayscale + index * 2}%)`,opacity:1,})})